
// const isRaining = true
// isRaining = false //cannot re-assign const
// console.log(isRaining)

const person = {
    name: 'Dan'
}

// person = {} // cannot reassign object
person.name = 'me' // but can manipulate key values in the object
console.log(person.name)
