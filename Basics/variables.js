let petName = 'Will'
petName = 'Doggo' // re-assign variable value, let has to start with letters, dollar, underscore. Cannot assign variables twice

console.log(petName)

// cannot use reserved keywords, such as let let
